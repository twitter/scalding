package com.twitter.scalding

import org.scalatest.{ WordSpec, Matchers }

class NoticeSpec extends WordSpec with Matchers {
  "This package" should {
    "not actually run tests but provide support for other subprojects" in {
      true === true
    }
  }

}